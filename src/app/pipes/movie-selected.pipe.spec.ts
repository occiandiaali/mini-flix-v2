import { MovieSelectedPipe } from './movie-selected.pipe';

describe('MovieSelectedPipe', () => {
  it('create an instance', () => {
    const pipe = new MovieSelectedPipe();
    expect(pipe).toBeTruthy();
  });
});
